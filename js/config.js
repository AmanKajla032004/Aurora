// ─── Local Dev Only ────────────────────────────────────────────
// This file is in .gitignore — it never gets pushed to GitHub
// The app reads the key from Firestore instead (saved via saveApiKey.html)
//
// OPTIONAL: You can paste a key here for local dev if you prefer,
// but it won't be needed if you've run saveApiKey.html at least once.
//
export const GEMINI_KEY = "";   // ← leave blank, use saveApiKey.html instead
